<<<<
          tr.innerHTML = `
          <td>${i + 1}</td>
          <td><span class="chip sev-${issue.severity}">${issue.severity}</span></td>
          <td>${escapeHtml(issue.area || '')}</td>
          <td>${escapeHtml(issue.route || '')}</td>
          <td>${escapeHtml(issue.test || '')}</td>
          <td>${escapeHtml(issue.status || '')}</td>
          <td>${escapeHtml(issue.title || '')}</td>
          <td>${escapeHtml(issue.evidence || '')}</td>
          <td>${escapeHtml(issue.fix || '')}</td>
        `;
====
          tr.innerHTML = `
          <td>${i + 1}</td>
          <td><span class="chip sev-${issue.severity}">${escapeHtml(issue.severity)}</span></td>
          <td>${escapeHtml(issue.area || '')}</td>
          <td>${escapeHtml(issue.route || '')}</td>
          <td>${escapeHtml(issue.test || '')}</td>
          <td>${escapeHtml(issue.status || '')}</td>
          <td>${escapeHtml(issue.title || '')}</td>
          <td>${escapeHtml(issue.evidence || '')}</td>
          <td>${escapeHtml(issue.fix || '')}</td>
        `;
>>>> 

<<<<
          const tbody = $('issuesTable').querySelector('tbody');
          tbody.innerHTML = '';
====
          const tbody = $('issuesTable').querySelector('tbody');
          tbody.textContent = '';
>>>> 

<<<<
          $('totalCount').innerHTML = String(total);
          $('criticalCount').innerHTML = String(critical);
          $('routeCount').innerHTML = String(routeCount);
          $('testCount').innerHTML = String(testCount);
====
          $('totalCount').textContent = String(total);
          $('criticalCount').textContent = String(critical);
          $('routeCount').textContent = String(routeCount);
          $('testCount').textContent = String(testCount);
>>>>