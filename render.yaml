services:
  - type: web
    name: kelionai-v2
    runtime: node
    envVars:
      - key: PORT
        value: "8080"
    preDeployCommand: mkdir -p server/data && chmod -R 777 server/data
    buildCommand: npm install && npm run build
    startCommand: npm run server:start
