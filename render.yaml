services:
  - type: web
    name: kelionai-v2
    runtime: node
    envVars:
      - key: PORT
        value: "8080"
    preBuildCommand: mkdir -p server/data
    buildCommand: npm install && npm run build && npm run server:install
    startCommand: npm run server:start
