require('dotenv').config(); const { createClient } = require('@supabase/supabase-js'); const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_SERVICE_KEY); async function run() { console.log('Deleting attack data...'); const {data:d1, error:e1} = await supabase.from('traffic_log').delete().like('path', '%.php%'); const {data:d2, error:e2} = await supabase.from('traffic_log').delete().like('path', '%.ini%'); const {data:d3, error:e3} = await supabase.from('traffic_log').delete().like('path', '%.env%'); const {data:d4, error:e4} = await supabase.from('traffic_log').delete().like('path', '%/.git%'); console.log('Cleanup PHP:', e1||d1); console.log('Cleanup INI:', e2||d2); console.log('Cleanup ENV:', e3||d3); console.log('Cleanup GIT:', e4||d4); } run();
