[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm ci --include=dev",
  "cd server && npm ci --omit=dev"
]

[phases.build]
cmds = ["echo 'using pre-built dist from git'"]

[start]
cmd = "cd server && npm start"
