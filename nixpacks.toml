[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "npm install",
  "cd server && npm install --omit=dev"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server/src/index.js"
