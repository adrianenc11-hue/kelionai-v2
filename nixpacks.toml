[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm ci --include=dev",
  "cd server && npm ci --omit=dev"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "cd server && npm start"
