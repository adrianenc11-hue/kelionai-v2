[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "cd server && npm install --omit=dev"
]

[phases.build]
cmds = ["echo 'Using pre-built dist from git'"]

[start]
cmd = "cd server && NODE_ENV=production node src/index.js"
