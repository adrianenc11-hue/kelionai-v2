[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "npm ci",
  "cd server && npm ci --omit=dev"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server/src/index.js"
