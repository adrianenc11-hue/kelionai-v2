AvatarSelect:
- useEffect sets z=1.4 directly via rotation.set()
- useFrame then uses armRot state (default z=1.2)
- BUT the useFrame OVERWRITES the useEffect values!
- So useFrame with z=1.2 is what actually shows
- Yet AvatarSelect works... WHY?

Wait - AvatarSelect DEFAULT is also z=1.2 and it WORKS there.
The difference must be something else:
- AvatarSelect: scale={2.0} position={[0, -2.2, 0]}
- LandingPage: scale={1.6} position={[0, -1.6, 0]}

Scale shouldn't affect bone rotation.

The REAL issue: maybe the deploy hasn't updated yet, or browser cache.
Let me try a hard refresh / cache bust.
